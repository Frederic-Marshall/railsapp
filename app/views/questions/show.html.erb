<div class="container">
    <h2>Конспект</h2>
    <%= render 'question', question: @question %>
    <div class='div-in-line-button'>
        <p><%= link_to "Все вопросы", questions_path, class: "q-b button" %></p>
        <% if @question.file.present? %>
            <p><%= link_to 'Скачать файл', @question.file, download: '' , class: 'q-b button' %></p>
        <% end %>
    </div>
</div>